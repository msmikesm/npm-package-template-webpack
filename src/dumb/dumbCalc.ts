/**
 * Dumb calc
 * @param numA number 1
 * @param numB number 2
 */
export const dumbCalc = (numA: number, numB: number): number => {
    numA = numA || 0;
    numB = numB || 0;
    return numA + numB;
};
